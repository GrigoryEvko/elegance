open Printf

let classify items limit =
  match items with
  | [] -> limit
  | [ x ] -> x
  | _ -> failwith "impossible bucket"

let relocate node = move_entry node true false

let unsafe_cast v = Obj.magic v

let erase v = Obj.repr @@ freeze v
